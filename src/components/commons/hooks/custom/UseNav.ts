const UseNav = () => {
  const navContents = ['BRAND', 'CATEGORY', 'LIFE', 'BEATY']
  const navInfos = ['#STYLE', 'EVENT', 'BEST']

  return {
    navContents,
    navInfos,
  }
}

export default UseNav
